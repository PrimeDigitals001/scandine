/**
 * PLACEHOLDER — replaced by generated types after the first push:
 *   npx supabase gen types typescript --linked > src/lib/supabase/database.types.ts
 *
 * Until then this keeps the typed clients compiling. Do NOT hand-maintain this
 * file once the real schema types are generated (CLAUDE.md §7).
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
